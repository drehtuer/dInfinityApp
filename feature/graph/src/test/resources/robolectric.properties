# Robolectric runs the app against this API, not against targetSdk.
#
# 4.17 cannot start API 37: its InputManager shadow still calls
# InputManager.getInstance(), which API 37 removed. Raise this to 37 once
# Robolectric supports it — the app itself targets 37 either way.
sdk=36
